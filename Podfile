# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Firebase-email-login-SwiftUI' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

pod 'Firebase/Analytics' 
pod 'Firebase/Auth' 


  # Pods for Firebase-email-login-SwiftUI

  target 'Firebase-email-login-SwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Firebase-email-login-SwiftUIUITests' do
    # Pods for testing
  end

end
